<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pooja.borkar.user_fitness">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activities.ExpertProfile.ExpertProfileActivity"></activity>
        <activity android:name=".activities.myplans.MyPlansActivity" />
        <activity android:name=".activities.MySubscribers.MySubscribers" />
        <activity android:name=".activities.ExpertsType" />
        <activity android:name=".activities.ExpertsList" />
        <activity android:name=".activities.ExpertsPlans" />
        <activity android:name=".activities.AppFeatureActivity" />
        <activity
            android:name=".activities.WelcomeActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.MainActivity" />

        <meta-data
            android:name="com.razorpay.ApiKey"
            android:value="rzp_test_7TueQkJLT4rpNl" />
    </application>

</manifest>